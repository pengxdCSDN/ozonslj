# Ozon 智能运营工作台

本上下文定义 ozonslj 多组织 SaaS 中统一使用的业务语言，覆盖卖家账户、店铺运营、选品研究、审核执行和智能运营协作。

## 身份与组织

**用户（User）**：
可登录本产品的人员身份；用户只有成为组织成员后才能访问组织资源。
_避免使用_：客户、卖家账户、运营人员（泛指所有用户时）

**组织（Organization）**：
拥有成员、卖家账户和工作区的 SaaS 租户边界。
_避免使用_：工作区、店铺、项目

**组织成员（Organization Member）**：
用户在某个组织内的成员关系，包含组织角色和成员状态。
_避免使用_：用户权限、工作区成员

**运营人员（Operator）**：
在获授权工作区内执行店铺运营活动的组织成员。
_避免使用_：所有用户、管理员、卖家账户

**工作区授权（Workspace Membership）**：
组织成员访问指定店铺工作区的明确授权关系。
_避免使用_：组织成员、角色

## 店铺与交易

**卖家账户（Seller Account）**：
由 Ozon Seller 身份及其授权凭据代表的卖家业务主体。
_避免使用_：用户、店铺工作区、广告账户

**店铺工作区（Store Workspace）**：
组织内围绕一个卖家账户开展运营、保存事实和执行授权的业务边界。
_避免使用_：组织、租户、项目

**商品报价（Product Offer）**：
由卖家报价编号标识的商品销售记录，可关联 Ozon 商品编号。
_避免使用_：条目、Listing 草稿、库存位置

**库存位置（Stock Position）**：
商品报价在指定仓库和 FBO/FBS 履约方式下的库存事实。
_避免使用_：商品、库存商品

**客户订单（Customer Order）**：
买家发起的商业订单，一张客户订单可产生一个或多个履约单。
_避免使用_：履约单、发货单

**履约单（Posting）**：
Ozon 在 FBO 或 FBS 下处理和配送商品的履约单位。
_避免使用_：客户订单、包裹、发货单

## 数据与研究

**官方私有事实（Official Private Fact）**：
由 Seller API 或 Performance API 获得、仅属于授权卖家或广告账户的数据事实。
_避免使用_：公开样本、市场估算

**运营导入事实（Operator Imported Fact）**：
由运营人员通过受控文件导入的搜索词、成本、采购、在途或目标资料。
_避免使用_：官方事实、系统估算

**公开样本（Public Sample）**：
从获准公开页面在明确时间和范围内观察到的竞品展示字段样本。
_避免使用_：全市场事实、官方销量

**推导估算（Derived Estimate）**：
由系统基于带来源事实和版本化假设计算出的非官方结果。
_避免使用_：事实、精确市场数据

**竞品种子（Competitor Seed）**：
运营人员明确维护并获准进行公开采样的竞品商品地址。
_避免使用_：搜索结果、全站抓取目标

**研究运行（Product Research Run）**：
一次固定研究模式、数据快照、算法和假设版本的选品分析过程。
_避免使用_：同步任务、Agent 运行、决策书

**商品立项决策书（Product Decision Document）**：
由研究运行产生并等待人工确认的结构化商品立项草稿。
_避免使用_：自动采购单、研究运行

**Listing 草稿（Listing Draft）**：
尚未改变 Ozon 商品的标题、属性、卖点和描述版本。
_避免使用_：商品报价、已发布 Listing

## 任务、审核与执行

**同步任务（Sync Job）**：
为店铺工作区导入或刷新一类 Ozon 事实的有边界后台过程。
_避免使用_：卖家操作、研究运行、定时器

**操作预览（Operation Preview）**：
外部写入前展示目标、旧值、新值、数据时间和风险的只读快照。
_避免使用_：确认框、执行结果

**审核请求（Review Request）**：
携带操作预览和风险上下文、等待有权限人员批准或拒绝的业务对象。
_避免使用_：执行命令、建议

**执行命令（Execution Command）**：
审核通过后创建、具有幂等身份且可分项追踪的单次外部写入指令。
_避免使用_：审核请求、同步任务、Agent 建议

**卖家操作（Seller Operation）**：
针对店铺工作区发生并可审计的 Ozon 数据读取或写入行为。
_避免使用_：任务、请求、执行命令

## 智能运营

**智能运营助手（Intelligent Operations Assistant）**：
继承当前用户权限、只能生成分析、草稿、报告、建议和待审核请求的只读助手角色。
_避免使用_：超级管理员、自动执行器

**Agent 工作流（Agent Workflow）**：
由版本化步骤和参数化只读工具组成的智能运营流程定义。
_避免使用_：自由 Agent 对话、执行命令

**Agent 运行（Agent Run）**：
Agent 工作流在指定组织、工作区、时间窗和事实版本上的一次执行记录。
_避免使用_：研究运行、同步任务

**运营报告（Operations Report）**：
可追溯到事实、工作流和模型版本的日报、周报、月报或异常报告。
_避免使用_：原始数据、无来源模型回答
# 当前产品边界说明

当前产品是供同一团队使用的 Ozon 运营系统，不是正在交付的多组织管理平台。“组织（Organization）”当前仅指服务端配置绑定的内部数据隔离边界；运营人员无需选择、创建或管理组织。除非后续需求重新确认，不得据此派生组织邀请、组织切换、成员目录或复杂授权功能。
