# Ozon 卖家运营

本上下文描述面向 Ozon 跨境卖家的浏览器运营助手，以及项目统一使用的业务语言。

## 统一语言

**运营人员（Operator）**：
使用扩展查看或管理卖家数据的已登录人员。
_避免使用_：管理员、客户、用户

**卖家账户（Seller Account）**：
由 `Client-Id` 及其授权凭据代表的 Ozon 卖家身份。
_避免使用_：用户、店铺账号

**店铺工作区（Store Workspace）**：
一个卖家账户在本产品内的隔离视图，包含权限、缓存数据和操作历史。
_避免使用_：租户、项目

**商品报价（Product Offer）**：
由卖家通过报价编号标识的商品记录；在 Ozon 已建档时可关联 Ozon 商品编号。
_避免使用_：商品、条目、SKU

**库存位置（Stock Position）**：
商品报价在指定仓库和履约模式下的可用数量。
_避免使用_：库存商品、库存条目

**履约单（Posting）**：
Ozon 在 FBO 或 FBS 履约模式下用于处理和配送商品的履约单位。
_避免使用_：包裹、发货单、订单

**客户订单（Customer Order）**：
买家发起的商业订单，一张客户订单可能产生一个或多个履约单。
_避免使用_：履约单、发货单

**操作预览（Operation Preview）**：
执行写操作前展示确切目标以及变更前后值的只读摘要。
_避免使用_：确认框、试运行

**卖家操作（Seller Operation）**：
针对店铺工作区执行并可审计的 Ozon 数据读取或写入行为。
_避免使用_：动作、请求、任务

**同步任务（Sync Job）**：
为店铺工作区导入或刷新 Ozon 数据的有边界后台过程。
_避免使用_：定时任务、工作进程任务

**同步检查点（Sync Checkpoint）**：
店铺工作区内某类 Ozon 资源最近一次完整成功同步后确认的长期进度。
_避免使用_：分页游标、同步任务

**智能运营助手（Operations Agent）**：
根据运营人员的自然语言意图检索业务知识，并通过受控工具查询或操作店铺工作区的助手。
_避免使用_：数据库管理员、万能机器人、自动决策者

**运营知识库（Operations Knowledge Base）**：
供智能运营助手检索的项目文档、业务规则、Ozon 接口知识和经过授权的店铺业务资料集合。
_避免使用_：数据库、模型记忆、聊天记录

**智能体执行（Agent Run）**：
智能运营助手处理一次用户目标的有边界过程，包含意图、工具调用、审批节点、结果和审计。
_避免使用_：同步任务、卖家操作、对话消息
