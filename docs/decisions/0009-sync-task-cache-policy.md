# ADR-0009：同步任务响应禁止缓存

## 状态

已采用。

## 决策

同步任务创建、查询、历史、取消和重试接口统一返回 `Cache-Control: no-store`。

## 原因与边界

任务状态、失败摘要、工作区标识和操作结果属于运营工作台上下文，不应被浏览器、扩展宿主或中间代理复用到下一次会话。客户端仍可按 `Retry-After` 进行短轮询，但每次都必须重新请求服务端。

该策略不改变 PostgreSQL 的持久化职责，也不允许把任务状态迁移到浏览器缓存或 Redis；Redis 只负责可恢复投递和协调。
