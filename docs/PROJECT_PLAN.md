# Ozon 跨境电商运营插件开发计划

## 1. 项目目标

开发一个 Chrome Manifest V3 扩展，为 Ozon 跨境卖家提供商品、价格、库存、客户订单和履约数据的统一运营入口。可信 Python 后端负责保存凭据、访问 Ozon、持久化和审计。

## 2. 技术方案

- 扩展：React、TypeScript、Vite、Manifest V3。
- 后端：Python 3.12、FastAPI、Pydantic、Uvicorn。
- 数据库：项目内 SQLite 文件，不使用 Docker。
- 测试：pytest、临时 SQLite、Stub、后续 Playwright E2E。
- 版本管理：Git，主分支为 `main`。

## 3. 开发阶段

### 阶段 0：需求与契约

- 明确目标卖家、区域、FBO/FBS 范围和所需数据。
- 核验 Ozon 官方接口、权限和配额。
- 完成领域语言、需求、架构、数据库和 API 文档。

退出条件：业务边界与第一版接口契约可评审。

### 阶段 1：本地基础

- FastAPI、本地配置、健康检查、SQLite、调试配置和测试框架。
- Manifest V3 扩展、React 侧边栏和本地 API 客户端。
- Ozon Stub 与商品报价分页查询。

退出条件：开发人员可以本地启动、加载扩展并看到商品报价。

### 阶段 2：账户与工作区

- 运营人员会话。
- 卖家账户添加、凭据加密和验证。
- 店铺工作区隔离和切换。
- 认证与账户操作审计。

退出条件：多个卖家账户的数据与权限正确隔离。

### 阶段 3：只读 MVP

- 商品报价搜索与同步。
- 库存位置、客户订单和 FBO/FBS 履约单。
- 同步任务、部分失败、限流提示和最后同步状态。
- API、契约、集成和 E2E 测试。

退出条件：只读 MVP 达到需求和质量验收标准。

### 阶段 4：受控写入

- 每次只引入一种写能力。
- 提供操作预览、明确确认、幂等、审计和失败恢复。
- 优先评估价格或库存更新，不默认开放破坏性操作。

退出条件：每项写操作均完成风险评审和失败路径测试。

## 4. 当前进度

- 已完成 Git 仓库初始化。
- 已完成 FastAPI 与扩展基础工程。
- 已完成 SQLite 商品报价持久化。
- 已完成商品报价分页 API 与侧边栏展示。
- 已完成本地运行脚本、VS Code 调试配置和质量检查。
- 已补齐中文架构、需求、数据库、API 和开发规范文档。

## 5. 下一迭代

1. 定义并实现运营人员本地会话。
2. 设计本地凭据加密和密钥保管。
3. 实现卖家账户与店铺工作区表及 API。
4. 根据 Ozon 官方文档实现账户验证网关。
5. 增加认证失败、限流、超时和畸形响应契约测试。
6. 在侧边栏实现工作区添加与切换。

## 6. 尚待决策

- 本地运营人员认证方式。
- 本地加密主密钥的存储方式。
- 首批支持的 Ozon 区域和履约模式。
- 缓存刷新策略与数据保留期限。
- 未来是否需要云端部署及 PostgreSQL 迁移。
