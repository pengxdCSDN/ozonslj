# 当前功能实现状态快照

本文档记录仓库当前代码和测试已经证明的功能，不替代需求确认文档，也不代表真实 Ozon 账号已接入。

| 编号 | 当前状态 | 已验证闭环 |
|---|---|---|
| DAT-009 | 已开发 | Seller 商品映射、预览 API、工作区快照保存、PostgreSQL JSONB、前端预览/保存、API 测试 |
| DAT-010 | 已开发 | Seller 库存映射、重复仓位校验、预览 API、快照保存、前端预览/保存、API 测试 |
| DAT-011 | 已开发 | Seller 订单映射、重复订单校验、预览 API、快照保存、前端预览/保存、API 测试 |
| DAT-012 | 已开发 | FBO/FBS 履约映射、重复 posting 校验、预览 API、快照保存、前端预览/保存、API 测试 |
| DAT-013 | 已开发（适配端口） | ERP 统一模型、CSV 预览 API、前端预览页、重复 external_id 校验；未接入具体 ERP |
| SYN-009 | 已开发（Stub/端口） | 分页、重试、游标防死循环、成功后推进水位；真实账号 Worker 仍需授权和官方接口核对 |
| AI-003 | 已开发 | 销售分析、零基线不虚构百分比、只读建议和边界测试 |
| AI-004 | 已开发 | 库存覆盖、缺货/积压建议、零销量不虚构覆盖天数和边界测试 |
| AI-005 | 已开发 | ACOS/TACOS/ROI 分析、有限数值校验、只读建议和边界测试 |
| AI-006 | 已开发 | 竞品/选品样本估算、评分和计数边界、只读结论和测试 |
| AI-007 | 已开发 | 日报/周报/月报、异常/待办汇总、统计边界和测试 |
| AI-008 | 已开发 | 定时/事件/手动触发、字段互斥、只读边界和测试 |
| AI-009 | 已开发 | Agent 能力白名单、SQL/凭据/写入拒绝和测试 |
| AI-010 | 已开发（预览） | 通知渠道校验、敏感数据拒绝、重试限制和仅预览页面 |

## 2026-08-09 广告能力闭环补充

以下状态以当前代码、PostgreSQL 迁移、API、前端页面和测试为准：

| 编号 | 当前状态 | 已验证闭环 |
|---|---|---|
| ADS-001 | 已开发（Stub/凭据闭环） | Performance OAuth 凭据后端加密保存、状态查询、工作区页面和凭据测试 |
| ADS-002 | 已开发（Stub/只读） | 广告活动、关键词和否定词 PostgreSQL 幂等快照、同步/查询 API、工作区页面和网关测试 |
| ADS-003 | 已开发（Stub/只读） | 广告报表 PostgreSQL 幂等快照、指标边界校验、同步/查询 API、工作区页面和网关测试 |
| ADS-004 | 已开发（只读） | ACOS/TACOS/CPC/CTR/CVR/ROI 统一公式、输入与结果快照、工作区 API、公式展示和网关测试 |
| ADS-005 | 已开发（只读） | 指标公式、统计窗口、币种和完整度展示 |
| ADS-006 | 已开发（只读） | 关键词诊断分类、阈值、报告持久化、工作区页面和边界测试 |
| ADS-007 | 已开发 | 阈值配置版本化、工作区历史和页面 |
| ADS-008 | 已开发（只读） | 30 天测试/筛选/放量/优化建议日历和页面 |
| ADS-009 | 已开发 | 广告建议写入边界检查、拒绝记录、工作区历史和页面 |

## 真实外部适配边界

## 2026-08-16 第六组：选品与 Listing 可提前开发能力

| 功能 | 状态 | 验收内容 |
|---|---|---|
| 商品筛选与机会评分 | 已开发（本地/导入数据） | Explore 按最低分、最低搜索量、转化率和自有覆盖缺口筛选；评分规则确定性、结果可保存到 PostgreSQL；无真实 Seller 数据时明确标记为估算 |
| 关键词库 | 已开发（工作区持久化） | 来源、观察时间、语言、分层和商品范围归档与历史查询 |
| Listing 草稿 | 已开发（Stub/人工编辑） | 俄语标题、属性/FABE/描述建议、关键词分层和 Smart Search 结果均保留原稿，不自动写回 Seller |
| 风险检测 | 已开发（只读建议） | 风险词、堆砌、类目和属性问题生成报告；原文与建议分离 |
| 版本管理 | 已开发（工作区持久化） | 原文、修改后文本、差异和审核状态可追溯 |
| 人工确认门禁 | 已开发（Stub） | 受控发布要求审核状态和幂等键；只记录 Stub 命令与回读，不伪造真实 Seller 写入 |

第六组退出条件已满足：后端 Explore 筛选/API、关键词、风险、版本和受控发布回归测试通过；前端 TypeScript 检查与 Vite Web 构建通过。真实 Seller 商品字段契约、外部写接口和真实店铺验收仍等待店铺审核及凭据，不作为本阶段完成条件。

| 编号 | 当前状态 | 说明 |
|---|---|---|
| SYN-009 | Stub/队列闭环已开发，真实适配待外部授权 | PostgreSQL 任务状态、Redis 投递、租约、心跳、取消、恢复、重试、分页和游标防死循环均已有实现；真实 Ozon Seller API 分页字段、权限、限流和账号凭据必须在官方接口核对及账号授权后接入，当前 live 模式会主动拒绝启动。 |

SYN-009 另新增可注入的 `PaginatedSyncHandler` 和分页 Fetcher 端口，已用 Stub 测试验证多页计数、游标推进、重复游标终止、有限重试和指数退避；接入真实 Ozon 客户端时只需实现 Fetcher，不改变 Worker 的租约与最终状态持久化链路。

2026-08-16 Seller 同步回归再次通过 33 项：商品、库存、订单、履约映射，分页重试、重复游标保护、水位不推进、任务调度、Worker 恢复和任务状态均已验证。当前剩余工作是等待店铺审核后实现真实 Fetcher/官方接口契约验收，不重复开发平行 Stub。

## 2026-08-16 第七组受控写入验收

第七组在没有真实 Seller Key 的范围内已完成验收：差异预览、新鲜度门禁、人工审批、权限阻断、幂等键、价格批量数量/涨跌幅限制、利润线保护、分项执行结果、回读核对和全程审计均已有代码、PostgreSQL 适配器、API/UI 或领域测试。相关回归 32 项通过，云端应用镜像内模块检查通过，API `ready=200`，API/Worker/Scheduler 使用同一摘要 `sha256:629976c6…`。

受控写入当前仍是 Stub/审核闭环：批准只生成受控命令并记录审计，不调用真实 Seller 写接口；真实商品字段契约、权限和写入回读需店铺审核及凭据到位后单独验收。

DQ-002 已补齐统一新鲜度摘要字段：最后成功时间、统计窗口、延迟、记录数和错误摘要随 PostgreSQL JSONB 决策快照保存，并通过 API 类型和领域测试验证。

RES-001/RES-002 已补齐统一前端入口：工作台现在支持 CSV 文本和 XLSX 文件选择，复用字段映射、预览校验、指纹和提交导入 API；XLSX 文件在浏览器内转为 Base64 传输，不保存到浏览器持久存储。页面采用“说明—字段映射/数据输入—预览提交”的纵向分区，避免全局 `.import-panel` 网格规则把步骤卡压成窄列；该数据可用于跨境电商的选品判断、Listing 关键词优化和广告预算分析。

搜索词导入页面已统一为三步工作流：先映射 `term`、`volume`、`rate` 字段，再选择 XLSX 或粘贴 CSV，最后执行预览并提交。页面提供“加载示例 CSV”按钮，用于在不准备真实文件、不接触敏感数据的情况下查看映射和预览效果；对应示例文件为 `docs/examples/search-keyword-report-sample.csv`。提交仍沿用文件指纹幂等规则，相同文件不会重复生成实际导入批次。

AI-001 已补充按工作区读取当前启用模型适配器的 API；仅返回适配器、厂商、模型、地址和配置状态，不返回密钥。真实模型请求仍由后续具体适配器实现，当前配置层不绑定厂商。

REV-003 已补齐人工审批队列：新增按工作区查询待审批请求 API，前端支持刷新队列、创建请求和逐项批准；批准只更新 PostgreSQL 审批状态，不直接调用外部写适配器。

REV-005/REV-006 价格批量校验已增加批次内 SKU 唯一性约束，大小写和首尾空格归一化后重复即拒绝，避免同一批次产生冲突命令。

AI-010 新增通知模板预览 API 和前端入口，仅允许 headline、summary、severity、workspace 等白名单字段；未知字段和敏感字段会被拒绝，预览不会发送外部消息。

搜索词导入校验进一步收紧：`conversion_rate` 必须是 0%～100% 的有限百分比，保留原始展示文本用于回溯；超范围或非法值在预览阶段拒绝，不进入 PostgreSQL。

## 验证基线

- 后端 pytest：以最近一次完整运行结果为准。
- Ruff：`./.venv/Scripts/ruff.exe check backend`。
- mypy：`./.venv/Scripts/mypy.exe backend/app`。
- 前端：`./extension/node_modules/.bin/tsc.CMD -b extension/tsconfig.json --pretty false`。
- 自动化测试不访问真实 Ozon、真实公开页面或真实大模型。

## 2026-08-18 RAG 评测模型调用错误诊断

评测执行器已将 Embedding、Chroma、Reranker、文本模型、限流、超时、认证、模型不存在、非法响应和运行时异常统一为脱敏错误码；Worker 将主要错误码与数量回写 PostgreSQL，结果页显示主要失败原因和下一步处理提示。该链路不保存供应商原始响应、提示词或凭据。验收顺序固定为先处理错误码、运行 30 例，再运行 120/240 例；真实供应商稳定性仍需云端实际运行验证。

## 2026-08-16 第八组智能分析与 Agent 验收

第八组在当前无真实 Seller 店铺凭据、无外部通知渠道凭据的条件下，已完成可独立开发和验收的只读业务闭环：

| 能力 | 状态 | 验收内容 |
|---|---|---|
| 商品、库存、订单、广告只读分析 | 已完成（Stub/只读） | 参数边界、空数据、时间窗口、结果结构和 API 回归通过 |
| 商品机会与竞品/选品分析 | 已完成（Stub/只读） | 复用选品评分、关键词和风险结果，不执行外部写入 |
| 汇总报告 | 已完成（只读） | 多来源指标汇总、缺失数据提示和报告持久化/查询通过 |
| RAG 知识问答与证据 | 已完成（只读） | 引用返回、无证据安全拒答和知识问答 API 回归通过 |
| Agent 工具编排 | 已完成（只读） | 顺序编排、工具白名单和状态流转通过 |
| 权限与高风险拒答 | 已完成 | 只允许销售、库存、广告读取和报告/待办；SQL、凭据读取、外部写入均拒绝 |
| 定时/事件/手动触发 | 已完成（只读） | 触发参数校验、只读标记和非法触发拒绝通过 |
| Agent 审计 | 已完成 | 工具调用、拒答和触发结果保留可追溯记录 |
| 外部通知 | 已完成（预览） | 白名单字段、敏感字段拒绝、预览和安全边界通过；未发送真实消息 |

本地第八组回归共 44 项通过。云端应用容器模块导入检查通过，API `ready=200`；API、Worker、Scheduler 使用同一镜像摘要 `sha256:629976c612d2afd5bc057e531d55ac23aafde7fe60e8232554c5ae11db65f893`。

外部通知真实发送、基于真实 Seller 数据的分析和真实渠道授权仍需店铺审核、Seller Key 或通知渠道凭据后单独验收；本阶段不将 Stub/预览结果宣称为真实外部调用完成。

## 2026-08-16 第九组 Performance 广告 Stub/只读验收

第九组无需 Seller Key，已完成独立 Performance 凭据域下的模拟数据与只读业务闭环：

| 功能 | 状态 | 验收内容 |
|---|---|---|
| 广告活动、关键词和否定词 | 已完成（Stub/只读） | PostgreSQL 幂等快照、页面、字段映射和重复数据回归 |
| 广告指标导入与计算 | 已完成（Stub/只读） | 展示、点击、订单、销售额、花费导入；ACOS/TACOS/CPC/CTR/CVR/ROI 口径校验 |
| 异常诊断与建议日历 | 已完成（Stub/只读） | 关键词分层、阈值版本、异常诊断和 30 天测试/筛选/放量/优化建议 |
| 广告预算分析 | 已完成（Stub/只读） | 预算利用率、周期预计消耗、healthy/warning/at_risk/exceeded 状态和建议；不执行预算写入 |
| Performance 与 Seller 凭据隔离 | 已完成 | 独立凭据范围、密钥不回显、页面不展示令牌正文 |
| OAuth 状态 | 已完成（Stub/状态闭环） | 未配置、可刷新、已过期、OAuth 失败和无权限分别返回明确状态/错误码 |
| 模拟数据回归 | 已完成 | 本地第九组后端回归 33 项通过，前端 TypeScript 与 Vite 构建通过 |

真实 Performance OAuth 授权和广告读取仍需独立的账号权限与真实凭据；本阶段验收不将模拟数据或只读预览宣称为真实线上广告同步。

云端发布门禁状态：第九组提交 `de90e07` 已推送到 `codex/deployment-base-images`，但当前 ACR 新摘要 `sha256:b0aee5f4…` 内仍缺少 `advertising_budget.py`，且镜像 revision 仍为 `development`，判定为旧源码构建，未完成第九组云端验收。不得用该摘要报告发布完成；必须重新触发正确 source commit 的 ACR 构建并核对镜像内关键文件后再部署。

后续重建已完成：ACR 摘要 `sha256:bbfaeca85483d0689b22faf2a869c2c4eaa5ee6e7024bee4193de6494d6032c0` 内已包含第九组关键代码；API、Worker、Scheduler 已统一该摘要，API `live/ready=200`，Web 入口 `index-AseVkSlj.js` 和样式 `index-DxjgRheu.css` 已同步。旧 Web 目录保留为 `web.rollback-group9-20260816`。

## 2026-08-16 第十组：可观测性和运维

已完成代码与本地验收：API 指标出口、请求/模型调用耗时与错误率、Scheduler/Worker 任务计数、资源快照、发布后健康门禁和显式条件回滚脚本。详细接口与限制见 [OBSERVABILITY.md](./OBSERVABILITY.md)。

云端验收已完成：ACR 应用摘要为 `sha256:e2cde1e36edbf00876f6bba367f40cf2bc5534694ca9e40e46f6915b68d5367b`，API/Worker/Scheduler 摘要一致；`live/ready/ops=200`，指标出口、资源状态、Nginx 和 HTTPS 首页均通过。服务器既有 Web 静态变更未清理。

## 2026-08-16 第三优先级：RAG 质量正式运行

质量评测执行器、`fixed-rag-v2` 固定 30/120/240 分层、Recall@5/10、Precision@5、引用支持率、正确拒答率、安全/提示注入、越权、多意图和批量错误隔离已完成。评测结果页已接入运行历史、执行进度、错误数、指标快照和硬门槛结论；结果与运行状态持久化到 PostgreSQL。离线脱敏契约验收三档均通过，执行命令与硬门槛见 [RAG_QUALITY_RUNBOOK.md](./RAG_QUALITY_RUNBOOK.md)。

本地契约通过不等于真实供应商稳定性通过；生产真实模型持续验收仍必须在云端已发布知识版本和供应商配置可用时执行，并记录脱敏成功率、P95、限流/超时/5xx 和降级次数。

本次发布后云端真实配置探针已通过：Embedding HTTP 200、实际返回维度 1024；Reranker HTTP 200；3 个文本模型均 HTTP 200 且响应非空。当前仍需在已发布生产知识版本上执行 30/120/240 长窗口回归，才能把“真实供应商可达”升级为“生产质量稳定性通过”。

RAG 评测页面已解除 Seller 店铺状态门禁，并完成当前语料分页、按案例 ID/问题/状态/安全标签搜索；云端前端资源为 `index-mMZA99ig.js`，API/Worker/Scheduler 应用摘要为 `sha256:527327178e9a6e4b9e3179c4907fad1ca140126e202c04de966905a735f53a52`。

## 2026-08-18 评测结果报告交互补充

评测结果报告已抽取通用分页组件，与案例确认列表共用上一页、下一页、输入页码跳转和边界处理；运行历史按每页 5 个批次展示。运行门禁区新增“确认语料 → 选择规模 → 查看结果”执行说明，批次卡片展示可读的评测批次编号、确认进度、后台状态和门禁原因。`rag-eval-...` 仅是 PostgreSQL/日志关联用的内部追踪 ID，页面提供复制但不要求用户手动填写。门禁不足的批次明确显示“不会执行”，任务完成前显示“等待执行/执行中”，完成后再展示正式指标。

## 2026-08-18 评测任务持久化与自动去重修复

修复评测按钮重复点击产生多条 queued 记录、API 重启后任务永久等待的问题：同一组织同一评测规模存在 queued/running 活动批次时自动复用；门禁不足的点击不再创建运行历史；PostgreSQL 保存租约、尝试次数和恢复字段，Scheduler 通过独立 Redis Stream 唤醒，Worker 领取并回写结果。历史 blocked 批次标记为明确阻断状态，历史重复活动批次保留审计但标记为取消，不重复调用模型。


## 2026-08-09 ??????

??????? PostgreSQL?API??????????? Ozon ?????????????????????

- LST-001?LST-007???????????????????Search Attributes?FABE?Smart Search?????????????????????
- DQ-003?DQ-007???/?????/?????/?????????????????? PostgreSQL ??????
- RES-003?RES-005?RES-009??????????????????????????????????????
- ?????PostgreSQL schema ????? scripts/check.ps1??? pytest?Ruff?mypy?TypeScript ? Vite ?????

????????????????????????????????
知识中心的“知识问答”和“知识源管理”同样已解除 Seller 店铺状态门禁；店铺仍在审核时可以进入这两个页面，知识接口继续由登录态和服务端权限控制。

2026-08-17 门禁矩阵已统一：Performance OAuth/广告闭环、模型适配器、RAG 配置与评测、Agent 权限、外部通知和知识中心页面均不再依赖 Seller 店铺 active 状态；依赖商品/库存/订单/履约事实的运营、选品、Listing、同步、数据质量和 Agent 触发器页面继续保留 Seller 门禁。解除 Seller 门禁不等于放开后端权限或独立 Performance OAuth，页面仍显示未配置/未授权/暂无数据状态。

## 2026-08-17 OCR 扫描 PDF 分流

已完成 OCR 适配器、扫描件检测、前端状态展示、脱敏错误处理、页级正文到现有切片链路的接入，并同步 [RAG_OCR_PROCESSING](./RAG_OCR_PROCESSING.md) 与部署/故障流程文档。普通文本层 PDF 不调用 OCR；扫描 PDF 使用本地 Tesseract 返回 `ocr_extracted`，本地依赖缺失时返回 `ocr_required` 并阻断发布。

本地 OCR 契约及 PDF/知识切片回归通过；TypeScript 和 Vite Web 构建通过。完整后端回归有 2 项既有迁移版本断言失败（测试硬编码 102，当前仓库最新迁移为 103），与 OCR 改动无关，未修改无关基线。

云端已完成本地 Tesseract OCR 验收：提交 `d3a6e9b` 通过 GitHub 绑定的 ACR 自动构建，镜像摘要为 `sha256:aa4f1b37f4dadd4d6abfdb93724319b59e40c8146d2cda6e4250af8a78e7c891`；API/Worker/Scheduler 摘要一致，镜像内 OCR 二进制与中英文语言包存在，HTTPS `live/ready=200`，首页 `200`。服务器 ACR CLI 凭据无效，但不影响 GitHub 自动构建流程；后续仍禁止在服务器本地构建。
